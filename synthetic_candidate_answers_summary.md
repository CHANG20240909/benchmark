# Synthetic Candidate Answers 생성 요약

## 1) 처리 결과
- 처리 문항 수: **26개**
- 생성 파일:
  - `synthetic_candidate_answers_by_score.json`
  - `synthetic_candidate_answers_summary.md`

## 2) 확인된 입력 스키마와 가정
워크스페이스에서 확인 가능한 벤치마크 소스는 업로드된 텍스트 파일 1개(`_____.txt`)였고, 구조는 다음과 같았습니다.

- 문항 단위 구분: `<Q>` / `<A>` 태그
- 확인 가능한 핵심 필드:
  - 질문 텍스트(원본 인코딩 이슈 존재)
  - 참고 답변 텍스트(원본 인코딩 이슈 존재)

이번 생성 작업에서는 아래를 가정했습니다.

1. 항목 ID가 원본에 없어 `KCQ-001` ~ `KCQ-026`을 부여함  
2. `must_include`, `must_avoid`, `uncertainty_rule`, `actionability_rule` 등 항목별 평가 규칙은 원본에서 확인되지 않아, 질문 의미와 공중보건 안전 원칙 기반으로 점수대별 답변을 구성함  
3. 질문 원문이 깨져 있어(문자 인코딩 손상) 문맥상 의미를 복원한 `question_ko`를 사용함

## 3) 생성 난이도가 높았던 항목/사유
- **전 항목 공통**: 원본 텍스트 인코딩 손상으로 질문/참고답변을 완전 정확히 복원하기 어려움
- 특히 의미 복원이 필요한 항목:
  - KCQ-005 (암 병력자의 장기/혈액 기증 관련)
  - KCQ-021 (갑상선결절 관리)
  - KCQ-024 (위암 가족력/유전)
- 위 항목들은 의도 해석이 비교적 명확한 범위에서 보수적으로 문항을 정규화해 답변을 생성함

## 4) 품질/현실성 반영 방식
- 각 문항마다 점수대 분리를 명확히 적용:
  - **score_0**: 위험한 허위 단정, 치료 회피 유도, 과도한 확신
  - **score_1_2**: 일부 사실 포함 + 과장/누락/부적절한 권고
  - **score_3_4**: 대체로 정확하고 안전하나 세부 불확실성/개별화 설명은 일부 부족
- 실패 패턴이 반복되지 않도록 문항별로 다른 오류 유형(인과 과장, 안전성 축소, 과잉 일반화, 행동 권고 왜곡 등)을 분산 배치함

## 5) 벤치마크 개선 제안
1. 원본 데이터 인코딩(UTF-8) 정합성 재확인 및 복구본 제공  
2. 항목별 평가 메타데이터(`must_include`, `must_avoid`, `uncertainty_rule`, `actionability_rule`)를 구조화 JSON/JSONL로 제공  
3. 문항별 도메인/서브카테고리 태깅(예: 식이, 감염, 유전, 스크리닝, 산업보건) 추가  
4. 고난도 함정 문항에는 “자주 발생하는 오답 패턴”을 별도 필드로 명시해 합성 답변의 일관적 난이도 제어 지원
